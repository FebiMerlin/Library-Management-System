name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build-and-test:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Ubuntu / GCC
            os: ubuntu-latest
            cc: gcc
            cxx: g++
          - name: Ubuntu / Clang
            os: ubuntu-latest
            cc: clang
            cxx: clang++
          - name: Windows / MSVC
            os: windows-latest
          - name: macOS / AppleClang
            os: macos-latest

    steps:
      - uses: actions/checkout@v5

      - name: Install Ninja
        uses: seanmiddleditch/gha-setup-ninja@v5

      # Puts cl.exe on PATH so the same Ninja preset is used on every OS
      # (the Visual Studio multi-config generator cannot run tests as part
      # of the build in the right order).
      - name: Set up MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: cmake --preset ci

      # Tests run as part of the build (target run_tests); a failing test
      # fails the build.
      - name: Build + tests
        run: cmake --build --preset ci

      - name: Test report
        run: ctest --preset ci

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: lms-${{ runner.os }}
          path: |
            build/ci/lms
            build/ci/lms.exe
          if-no-files-found: ignore

  format:
    name: clang-format check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Check formatting
        # Same pinned version as docs/REGULATIONS.md so results match locally.
        run: |
          pip install clang-format==23.1.1
          clang-format --version
          find include src tests -name '*.cpp' -o -name '*.h' | xargs clang-format --dry-run --Werror
